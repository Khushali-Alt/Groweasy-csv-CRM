# If unset, the app runs fully offline using a heuristic mock mapper
# (utils/mockAiMapper.ts) instead of calling a real backend — useful
# for developing/demoing the frontend before the Express API exists.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
